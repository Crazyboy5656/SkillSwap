{
  "name": "skillswap-v2",
  "version": "1.0.0",
  "description": "Peer-to-peer skill exchange platform",
  "scripts": {
    "build": "node scripts/inject-config.js",
    "dev": "npx serve . -p 3000",
    "lint": "echo 'No build step — vanilla HTML/JS'"
  },
  "devDependencies": {}
}
